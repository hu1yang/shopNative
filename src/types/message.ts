export type optionMessageProp = {
    id?: number,
    message: any,
    type: number,
    roomId: number,
    sendUser: string,
    createBy?: number,
    status?: number,
    messageToken?: string,
}
