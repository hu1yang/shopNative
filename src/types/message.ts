export type optionMessageProp = {
    id?: number,
    message: any,
    type: number,
    roomId: number,
    sendUser: string,
    receiveUser: string,
    createBy?: number,
    status?: number,
    messageToken?: string,
}
